events { }

http {
    upstream node_app {
        least_conn; # Load balancing method
        server :13694;
        server :13695;
    }

    server {
        listen 13693;
        
        location / {
            proxy_pass http://node_app;
        }
    }
}