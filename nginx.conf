events { }

http {
    upstream node_app {
        least_conn; # Load balancing method
        server localhost:13694;
        server localhost:13695;
    }

    server {
        listen 13693;
        
        location / {
            proxy_pass http://node_app;
        }
    }
}